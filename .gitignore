# IDE
.idea/
*.iml

# Documents personals d'origen (no necessaris per a l'app desplegada)
# (només l'arrel; els PDFs de reserva que fa servir l'app viuen a public/docs/t/)
/indonesia/
/vols-david.pdf
# Carpeta de tickets/documents d'origen — conté passaports, DNIs, assegurances,
# etc. NO s'ha de pujar mai.
/documents-drive/

# Tickets/reserves de l'app: es despleguen a Firebase des del disc, però NO
# es pugen a git (informació confidencial i el repo és públic).
/public/docs/

# Firebase
.firebase/
firebase-debug.log

# OS
.DS_Store
Thumbs.db

# Node (per si s'hi afegeix tooling)
node_modules/
